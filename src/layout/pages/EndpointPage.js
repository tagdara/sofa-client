import React  from 'react';
import EndpointLayout from 'endpoint-model/endpoint/EndpointLayout'

const EndpointPage = props => {

    return (    
        <EndpointLayout {...props} />
    )
}

export default EndpointPage;
