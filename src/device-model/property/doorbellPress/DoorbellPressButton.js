import React from 'react';
import { Button } from '@mantine/core';

const DoorbellPressButton = props => {

    return (
        <Button disabled={true}>{"pressed"}</Button>
    )
}

export default DoorbellPressButton