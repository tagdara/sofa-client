import React from 'react';
import { Badge } from '@mantine/core';

const DoorbellPressBadge = props => {
    
    return (
        <Badge>{"pressed"}</Badge>
    )
}

export default DoorbellPressBadge