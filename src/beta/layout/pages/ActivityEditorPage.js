import React from 'react';
import ActivityEditor from 'beta/activity/editor/ActivityEditor'

// This is just a placeholder to put a module in the lazy load page directory

export default function ActivityEditorPage(props) {

    return (
        <ActivityEditor {...props} />
    )
};
