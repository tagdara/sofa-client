import React from 'react';
import ModeSelect from 'beta/device-model/property/mode/ModeSelect'

const Mode = props => {

    return (    
        <ModeSelect {...props} />
    )
}

export default Mode;